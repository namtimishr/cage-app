import React from "react";
import ReactDOM from "react-dom/client";
import CageConsole from "./CageConsole.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <CageConsole />
  </React.StrictMode>
);
